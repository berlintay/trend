an embed to created auto-welcome to new joins.
so if (!embedwelcome) {
    console.log(`${user.bot.tag} in`)
    message.reply('${user.mention}, please provide the embed code')
  }

  // so on so forth 
